# kubenetes

- Deployment:
  + strategy
  + replica
  + pod
  + selector
    
- Pod:
  + containerPort
  + image

- Service
  + type
  + selector
  + port
  + targetPort #portContainer
  + nodePort
  
- ResourceQuota:
  + cpu
  + memory
  + namespace #hạn chế resource ở namespace này
